#include "main.h"
/**
 * _islower - check whter valur is lower or not
 * @c: int value
 * Description: return 1 if condition is true
 * Return: int value
 **/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
